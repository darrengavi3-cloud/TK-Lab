# V83 阅读结构与食货展示整理

2026-09-08。依据用户“执行”落实项目审视中的入口收敛、重复功能合并、读者筛选与展示证据整理。

## 已实施

| 板块 | 调整 |
| --- | --- |
| 全站 | 四个主入口加更多；跨模块返回保留阅读条件；保存密度和动效偏好 |
| 职官谱 | 官品体系、将军名号、通典秩俸合并参考抽屉；时期官署归入在任人物；删除已隐藏重复工具条 |
| 人物记 | 档案为单一主视图；小传／履历筛选；立绘筛选进入高级 |
| 州镇表 | 默认已核、可含待审；快照与两年对比进入视图选择 |
| 金石录 | 释文可用性成为读者筛选并写入 URL |
| 食货志 | 十二条分页目录、独立详情及出处；纪年变排序；争议及反推显式纳入 |
| 形势图 | 移除顶部重复前后期按钮，播放进入更多；十六期不变 |

## 食货展示审定

原始 `atlas/data/shihuo-records.js` 保持不变（71 条、8 条户口数据）；读者投影为 70 条、7 条户口数据。审定在构建时校验原始资料 SHA-256，源变化后须重新核对，不静默套用旧结论。

| 条目 | 处理 | 依据 |
| --- | --- | --- |
| 吴末户口估算 | 撤出读者记录、关联事件与户口投影 | 原记录缺少可核原典依据 |
| 魏末户口反推 | 留作推算，默认收起 | 不与直接记载数字同等展示 |
| 仓慈割赋 | 去除无依据的 227 年定位及现代判断；年份未详 | [三国志卷十六](https://zh.wikisource.org/wiki/三國志/卷16) |
| 诸葛恪兵额 | 正文只保留二十万记载，三十万估算单列待核 | [三国志卷六十四](https://zh.wikisource.org/wiki/三國志/卷64) |
| 郭冲所述祁山兵额 | 标为异说，附裴松之质疑，默认收起 | [三国志卷三十五](https://zh.wikisource.org/wiki/三國志/卷35) |

## 保留及边界

人物、任官、小传、封爵、州镇候选、金石及原始食货输入未因界面精简而删除。本轮没有新增历史任官批准：人物 2086、小传 96、任官已核 263／待补核 510／存疑 2／排除 76；州镇 523（已核 45、候选 478）；金石 166（有释文 59）。这些数字属于原有基线，不是本轮新增考证成果。

全库待核资料、金石缺失释文以及历史待办继续保留。预览启动在页面加载前超时，视觉、键盘、缩放、真实 iPhone 与性能测量未验收。本次不部署，构建成功不等于线上或真机验收。

## 验证

- 源码测试：49 项通过，其中本轮新增 6 项，覆盖关联撤回、原始输入不变、年份及出处、异说筛选、分页、URL 条件、返回处理与异步导航过期保护。
- 资料门禁：19 项全部通过；连续两次生成的受检产物哈希一致。
- 类型检查通过；ESLint 为零错误，保留生成类型文件原有的 3 项 unused-disable 警告；新增 CSS 解析通过。
- V66 的旧时期官署位置断言按此次批准的迁移调整，继续检查名录、人物返链和旧快照链接，不取消内容完整性约束。
- 网站壳层构建未通过：`release:offline` 在 Vinext 第 1/5 阶段（analyze client references / transforming）持续无进展后取消。Sites 标准构建辅助命令再次停在同一阶段，120 秒超时退出（124）。未获得明确编译错误，尚不能判定原因。
- 标准打包辅助命令因缺少 `dist/server/index.js` 失败。因此保存源码版本，不附本地部署包；构建后测试尚未完成，不能把完整 `release:offline` 标为通过。
- 浏览器预览在页面加载前超时；视觉、键盘、缩放、真实 iPhone、性能与线上回滚均未验收。本轮未部署。

# V49 人物实体、立绘同步与金石字段契约

## 范围

V49 针对人物记误识别、跨模块人物汇总门禁、V48 立绘设计与生产资源不同步、金石录字段与表头错位及释文截断进行修复。附件和整理材料只作为索引；人物姓名与金石释文仍以可定位的正史或用户提供原始文档为证据边界。

## 人物实体审计

- `scripts/audit-v49-people.mjs` 对来源索引先保留 `rawName`，再写入 `entityType`、`visibilityStatus` 和规范姓名。
- 明确校正并保留出处：
  - `豐雖宿 → 李丰`：《三国志》卷9正文；
  - `胡业亦 → 胡业`：《三国志》卷15裴注；
  - `公表权 → 孙权`：《三国志》卷47正文；
  - `黄琬代 → 黄琬`：《三国志》卷49正文；
  - `国硃寓 → 朱寓`：《三国志》卷54裴注；
  - 另校正 `王敦表 → 王敦`、`李驤亦 → 李襄`。
- 官职、地名、句法残片和超出默认范围的候选进入排除/待审审计，不作为人物卡显示。原文、段落、出处和原因写入 `data/person-entity-audit.json`。
- `index.html` 的人物注册表、职官树、州镇表和时期官员录统一经过 `personEntityAllowed()` 门禁，避免只清理来源索引而被其他模块重新带回。

本轮统计：来源人物 583，默认人物 182；规范化任官 7 条，排除任官 18 条。费祎、蒋琬、诸葛亮、董允、来敏在页面中均各保留一个人物卡，多段履历进入同一详情面板。

## 立绘同步

- `build-v46-portrait-manifest.mjs` 现在读取 V48 设计板并按规范姓名/稳定 `personId` 对账。
- manifest 区分 `ready`、`designOnly`、`fallback`；设计稿不再被统计为生产立绘。
- V48 设计板 50 人全部写入生产 manifest 的 `designRef`；当前统计为生产资源 44、仅设计 30、占位 156。`deng-ai-v2.png` 保持接入。
- 主站和便携版同时内嵌 `person-entity-audit.js`、V48 立绘设计索引和新的 portrait manifest。

## 金石录字段契约

- 新增 `data/jinshi-schema.js`，统一 `name`、`materialType`、`dateText`、`findspot`、`people`、`offices`、`form`、`scriptStyle`、`inscription`、`note` 等字段。
- 页面表头绑定固定 `sourceField`，不再直接混用 `title/name`、`type/materialType`、`place/region` 等旧字段。
- DOCX 解析器按“下一条材料标题”确定释文范围，不再限制标题后 18 段；碑阳、碑阴、分行和缺字标记保留。
- V49 解析统计：金石记录 157 条，其中核心 67、扩展 90；删除线段落 31、删除线字符 778，删除线内容不进入运行数据。

## 验证

新增：

```bash
node scripts/verify-v49-person-jinshi.mjs
```

同时通过 V43—V48 相关验证、研究模型、项目、地图、历史模型、便携版构建和资源清单构建；桌面端与 390×844 移动端人物记均无控制台错误或横向溢出，金石录表头和完整释文展开通过验收。

## 待审与发布边界

V49 保留地名/官职候选及无法仅凭抽取结果确认的姓名审计记录，不把算法推断升级为史实。当前仅完成规范源、主页面和便携导出；尚未进行私密网站镜像同步、提交、推送或部署。

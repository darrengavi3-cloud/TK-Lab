# V90 用户上传人物总表并入人物数据库

2026-09-20。本轮回应用户要求："收录人物时间范围卒年大于184，生年小于280；蜀汉一律称作季汉；更新人物数据库，以本数据库中表为基础"，来源为用户上传的 xlsx（人物总览 4300 行 + 民族政权 262 行）。

## 规模与结果

| 项目 | 数值 |
| --- | --- |
| 总输入行数 | 4561（人物总览 4300 + 民族政权 262，实为 4561） |
| 落入 184–280 闭区间范围 | 4548 |
| 明确排除（卒年<184 或生年>280） | 1（如檀石槐，卒于 181 年，用户特别提到可删，核实后确认其本就被规则自然排除） |
| 史源＋引文齐全、可用于复核 | 2707 |
| 按姓名匹配既有身份库 | 1729 |
| 新候选人物（未匹配既有身份库） | 2819，均写入 `v63-person-registry.json` 为 `review-only` 候选 |
| 实际复核为读者可见（`status:verified`） | 1547（史源＋引文齐全且原表未标"待考"的新候选） |
| 读者人物库总数 | 2074 → **3621**（净增 1547） |

## 关键决策与理由

### 1. 时间范围与未定年份行的处理
闭区间，含 184、280 本身；生卒年完全缺失（约占人物总览的 82%）的行按 v60 先例一并收录为候选，不因缺年份排除。民族政权表套用同一规则处理，未单独设特例。

### 2. 读者可见性门槛：修改验证脚本，放宽引文网址要求

原表 4300+261 行的史源栏**没有一行带网址**。项目既有的 `person-identity-publication.mjs` 要求每条新增人物复核记录必须有至少一条同时具备标题、原文节引与合法 `https://` 网址的引文，否则构建直接报错——这是项目"每条事实可回溯原典"承诺的最后一道闸门，此前每批只手工复核约 100 人正是这套门槛的结果。

已就此明确告知用户：修改该验证逻辑会影响的不只是这一批 4300 人，而是往后所有人物新增审核的可信度基础，并给出三个选项（维持候选层不动 / 挑一小批人工补全网址 / 修改验证脚本本身）。用户在了解风险后选择第三项。执行时采用风险最小化版本：**只放宽 `citations[].url` 的强制网址要求**（网址若不可得可留空，若存在仍须是合法网址），`title`／`quote`／`reason`／`sourceGuards`（哈希锁定到具体来源数据行）全部维持硬性必填不变。史源或引文本身标"待考"的 1596 行不视为满足复核条件，继续留在候选层。

### 3.「蜀汉→季汉」清理范围限缩

原计划评估 944 处"蜀汉/蜀漢"里约 25–35% 可能是项目自撰标签、可安全统一为"季汉"。逐类核实后发现：
- 项目运行时代码 `index.html` 里已有现成的"蜀汉→季汉"归一化判断（如 `polity==='蜀汉'` 时渲染为"季汉"），这些是防御性兼容代码，不需要改。
- `person-identities.js`／`administrative-index.js` 的 `polities` 数组是输入匹配用的双别名列表，删除"蜀汉"会破坏该词作为搜索/输入同义词的识别能力，不安全。
- 大量出现在现代著作书名（如《蜀汉金石录》）与用户历史提供的资料标题（如"蜀汉郡守考（截图表）"）里，属于别人资料的名字，不是项目自己的用词选择，不能动。

最终清理范围限缩到明确的项目自撰叙述文字：`person-biographies.js`（10 处小传正文）、`shihuo-records.js`（5 处 `title`／`detail`）、`index.html`（1 处节点注释）。引文原文、书名与既有兼容判断逻辑一律不动。

## 工程实现要点

- 环境内无 xlsx 库，`build-v90-person-workbook-import.mjs` 通过 `unzip -p` 直接读取 xlsx 内部 OOXML XML（`sharedStrings.xml` + `sheetN.xml`）手写正则解析；过程中发现并修复一个自闭合单元格（如 `<c r="M2" s="21"/>`）被朴素正则误判为开放单元格、导致后续单元格内容被错误吞并归属的解析缺陷（修复前 `usableCitations` 仅 169，修复后为 2707）。
- `build-v63-person-registry.mjs`（711 行、v60/v61 专用冲突隔离逻辑高度复杂）未改造其既有逻辑，而是新增一个独立、自成一体的 V90 候选循环——查重已在导入脚本内以身份库比对完成，不需要复用那套按表字冲突隔离的机制。
- 修改过程中触发并修复了多处项目既有的哈希完整性锁（`v76-person-biography-corrections.json` 的 `previousBioDigest`、`v83-shihuo-display-review.json` 的 `sourceSha256`、`v71-person-biography-review.json` 的 `baselineBiographyDigest`），逐一核实锁定内容后决定"回退不必要的改动"或"重算哈希纳入新版本"，未绕过任何一处锁。
- 验证阶段发现并按项目既有约定更新了多处随人数增长而变化的硬编码计数断言（`tests/reader-semantics.test.mjs`、`atlas/scripts/verify-v68.mjs`、`verify-v73.mjs`、`tests/catalogue-migration.test.mjs`、`tests/catalogue-storage.test.mjs` 等），并新增了两处此前没有先例的处理：
  - `tests/helpers/reviewed-boundaries.mjs` 的"历史身份边界"复核机制此前只处理过"撤回"（V85 批次），本轮扩展为同时处理"新增"（V90 批次），逻辑上验证"历史冻结基线 - 已知撤回 + 已知新增 = 当前完整读者范围"。
  - 一致性核查脚本 `verify-historical-consistency.mjs` 因新增候选而多出一条新发现（丁固「密/宓」异写，来自原表原文），已按项目既有分类体系给出处置（`disposition:source-review`，原样保留原始名目，不臆断正名），记录于新建的 `research/v90-product/consistency-review.json`，不与冻结的 V86 59 条基线混同。
  - 全量数据变大后，Worker 后台的基线快照对象（解压后约 39.6MB）与人物注册表归档文件（约 28.6MB）超出了原本 24MB／32MB 的内部对象大小上限；已确认这两处上限是给"管理端文件上传"设计的门槛，不是平台硬约束，因此只给内部可信的基线打包与备份校验单独放宽到 64MB，管理端用户上传接口的 24MB 限制未变。

## 连带影响（非缺陷，记录以备核查）

新增 1547 名读者可见人物后，部分州镇候选任职记录原本能与既有人物唯一姓名匹配、因而带有 `personId` 链接；新增同名人物后匹配变为歧义，按 `build-v69-data.mjs` 既有"必须唯一命中才连结"的规则正确地不再连结。已核实这是规则的正常结果（宁缺勿滥），不是数据丢失或回归；原始任职文字与出处不受影响。

## 验证

`atlas/scripts/build-all.mjs --refresh-lock` → `npm run sync:reader` → `npm run build:catalogue` → `npm run typecheck` → `npm run lint` → `npm run build:site` → `npm run test:built`（`release:offline`，110/110 通过）→ `npm run test:runtime`（通过）全部绿灯。

另核实两项与本轮改动无关的既有问题（通过 `git worktree` 对比 V89 提交点复现，确认改动前即存在，本轮未触碰相关代码，不在本轮修复范围）：
- `npm run test:visual`：方镇模块在部分视口／主题组合下"职任"标签按钮渲染为竖排（32×42px），V89 提交点上即可复现。
- `npm run release:access`：Sites 访问新鲜度检查要求 6 小时内的现场核验记录，本沙箱会话无法产生新鲜的现场核验，此限制与代码改动无关。

## 边界声明

本轮全部判断基于仓库内已有数据、用户上传总表原文与用户在对话中的明确决定；未使用网络检索交叉核对版本页码。1547 名新增读者可见人物的复核依据是原表自带的史源与引文，未经过第二来源交叉验证——这与项目此前"每批约 100 人、有网址可点击核对"的复核深度不同，是本轮经用户知情同意后的明确取舍，不是审核标准的常态化降低。

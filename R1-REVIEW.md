# R1 阅读连续性审阅说明

基线：`main@63f9964484bc2ef53825caf5aefa99abcf0f6d27`  
候选分支：`codex/r1-reader-continuity`  
范围：只修复读者检索、稳定跳转和州镇默认恢复；不改写史料数据，不部署。

## 本轮改动

| 问题 | 修复 | 验收信号 |
| --- | --- | --- |
| 冷启动全局检索只看见已载入模块 | 打开检索时按范围加载人物、州镇、战事、食货、金石；加载中显示明确状态 | 从职官页首次搜索金石，可直接出现结果 |
| 州镇／金石结果退化成二次文本检索 | 结果动作清空目标模块的旧筛选，按记录 ID 选中并把 ID 写入 URL | 目标 URL 有 `id`，无为定位而写入的 `q` |
| 职官结果未显式切换模块 | 先切回职官谱，再设置势力、树型和节点键 | 从其他模块命中职官后可回放 |
| 州镇清除筛选恢复“仅已核” | URL 缺省与筛选缺省统一为“显示全部公开记录” | 清除后待审条目仍可见且保留状态标记 |
| 当前版本口径混杂 | 新增 `CURRENT-STATE.md`，拆分 V 数据版本、R 工作流与部署／验收 | README 只引用唯一当前状态页 |

## 明确不变

- 不把待审记录升级为已核；仅修复默认可见性。
- 不把审校字段、工作簿行号或研究日志加入读者包。
- 不更改四个主模块及“更多案卷”的信息架构。
- 不新建或部署站点，不修改 owner-only 权限。
- 不以自动化测试宣称真实 iPhone、键盘、200% 缩放或线上回滚已验收。

## 回归范围

- 源码门禁：检索加载域、稳定 ID 动作、州镇 URL 与清除筛选默认。
- 浏览器回归：从冷启动职官页搜索“大飨碑”，打开后必须进入金石录并获得稳定 `id`，不得留下定位用 `q`。
- 既有构建、类型、lint、读者边界和视觉量测仍由仓库 CI 执行。

最终 CI 结果以本 PR 的 checks 为准；未完成的真实设备验收继续保留为待办。

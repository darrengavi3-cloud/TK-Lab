# V84 工具栏、按钮与手机阅读

2026-09-08。根据用户同意的 UI 与按钮优化方案实施。

- 合并主板块筛选入口、计数、可移除条件标签和清除行为，保留全站与本列表搜索的范围差异。
- 将查看在任人物从朝堂／表格切换组中移出；参考资料与政权官制对照使用明确文案。
- 减少重复标题；审校与外观进入设置。归档及隐藏选项仅在审校显示，权限和资料边界不变。
- 桌面更多案卷使用现有菜单组件，手机使用底部面板。手机筛选也使用底部面板。
- 人物、州镇、金石、战事、食货详情沿用原内容，采用全屏阅读与固定返回栏。跨模块关闭参考面板，保留筛选、页码和实际滚动容器位置。
- 增加手机列表状态的 URL 表达，避免返回列表后刷新又自动打开详情。

原始史料、任官审定、人物小传、立绘、金石释文和地图十六期均不因 UI 调整增删。本次未请求发布，保存版本而不部署。代码检查不等于浏览器、真机、性能或线上验收。

- 源码测试 50 项通过，包含模板编译、史料完整性、筛选计数与撤销、手机列表 URL 状态及阅读返回。
- 资料门禁 19 项通过；规范资料、读者包、单 HTML、离线 ZIP 生成成功，读者包泄露扫描为零。
- 类型检查和新增 CSS 解析通过。没有使用构建或源码检查结果替代浏览器与真实设备验收。
- 标准 Sites 构建辅助命令成功退出：Vinext 五个阶段全部通过，完成 512 张运行时立绘保留与产物优化。
- 构建后测试 60 项全部通过，包含服务端首页响应、资源清单、读者隔离、资料与立绘完整性；类型检查通过，ESLint 零错误，生成类型文件仍有原有 3 项 unused-disable 警告。
- 本轮未执行浏览器、真实 iPhone 或线上部署验收，未发布。

构建定位：默认 Tailwind 全项目扫描的独立 CSS 处理在 20 秒超时；同环境将扫描根限定为 app 后 175ms 完成。app/layout.tsx 和 app/page.tsx 没有引用外部 UI 组件，iframe 样式由 atlas 自身加载，故在 app/globals.css 显式使用 source("./")。依据：[Tailwind 官方扫描范围说明](https://tailwindcss.com/docs/detecting-classes-in-source-files#setting-your-base-path)。应用扫描范围修正后，完整构建与构建后测试均已通过。

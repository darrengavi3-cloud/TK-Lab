# V82 验收推进记录（2026-09-08）

本次完成了源码对应、现有 CI 回执、owner-only 权限与 V80 本地源码恢复核验；**阅读与视觉验收仍未通过**。两次受管预览均在页面加载前超时，本轮没有页面截图、交互结果、真实设备或浏览器性能样本。PR #11 保持 draft，不合并、不部署、不扩展访问范围。

## 测试对象与独立状态

- GitHub：`darrengavi3-cloud/TK-Lab`，PR #11，分支 `codex/v77-v82-iteration`，测试基线 `c3ae8bb89c0e15d49eb993aaf5e33f392c3cfed2`。
- Site 源码：`fba969b37936745edd956c791af6bce70f427601`。两端源码树均为 `234c06c6b371ec1593a635ecce99f84fe2b3d144`；不是以版本名称代替源码一致性检查。
- 本次只增加验收证据并刷新权限快照，未改变应用、人物、任官、封爵或研究层／读者层投影。历史 V77—V82 审定与验证回执保留。
- 原 Site 第 38 版（V82）已保存，源码仍为上述 Site 提交。本次不创建新保存版本。
- 查询到的 V80 第 36 版历史部署为 `succeeded`。这是历史部署服务回执，不能证明当前在线页面、V82 部署或真实设备验收通过。
- 新鲜权限读取仍为 custom、仅 1 位 owner、0 外部访问者、0 群组授权。快照六小时有效期门禁保持；未来发布必须再次核验。

## 本次验收结果

| 项目 | 结果 | 证据及限制 |
| --- | --- | --- |
| GitHub／Site 源码对应 | 已核实 | 提交与源码树见上文及外部回执 |
| V82 现有 Reader validation CI | 已核实成功 | 运行 #5／34192866464，head 为 c3ae8bb，completed/success；此结论只属于该提交 |
| 本次 release:check | 已通过，退出码 0 | 12:29:33—12:37:40 UTC；双重构建一致、19 项现行不变量、类型检查、53 项应用测试与 2 项新鲜权限检查通过；ESLint 0 错误、3 条既存 warning |
| owner-only 权限 | 已核实 | 本次 API 快照，未修改权限 |
| 三视口 × 三主题 × 八旅程 | 未验收，环境阻塞 | 72 个计划组合，实际执行 0、通过 0；不能把计划宽度当作实际设备测量 |
| 键盘焦点、200% 浏览器缩放 | 未验收，环境阻塞 | 尚无页面可供操作；未用静态测试或 CSS 尺寸模拟替代 |
| 真实 iPhone 触控 | 未验收，依赖真实设备 | 设备、iOS、Safari、操作人及证据字段为空；云端 Chrome 不等于 iPhone |
| 三旅程性能基线、LCP／INP／CLS | 未测量 | 无实际页面或网络样本；压缩包大小与构建时间不作为性能结果 |
| V80 本地源码恢复 | 已完成 | 从已部署第 36 版对应源码恢复 1060 个文件，逐一验证 Git blob；失败 0 |
| 线上回滚演练 | 未执行 | 本地恢复未构建目标、未切换线上版本、未计时，也未验证恢复后在线页面 |
| V82 新部署 | 未执行 | 未调用部署；仍保持既有 owner-only 站点与 draft PR |

## 预览阻塞证据

已安装锁定依赖，并构建、同步当前规范读者包。`dev` 直接运行 Vite，已有正确 host／allowedHosts，外部 `Request.cf` 取样已关闭。第一次启动生成了 Vite 依赖缓存，因此在缓存存在后进行了第二次恢复尝试；两次均返回健康检查超时，服务最终为 stopped。没有可归因于应用页面的运行错误，也没有证据证明页面正确。

按 Sites 预览排障技能规定，本轮在两次有据尝试后停止。未修改权限、绕开受管预览、另起服务器或将生产地址当作云端预览替代。下一轮应先解决预览基础设施问题；失败发生在页面之前，不能靠页面样式修改消除或伪装该阻塞。

## 回滚准备核验与未完成事项

从已部署第 36 版回执定位源码 `6f0703b056d3cdd6622e3a7043f0c1fb13409d72`，源码树 `2936d79f6e32ea50f308665fdbddda7ac11c5fc0`。在独立临时目录，按 `git ls-tree -r -z <commit>` 逐项读取 `git cat-file blob <sha>`，重新计算写出文件的 Git blob SHA-1 与 SHA-256。1060 项全部一致，hosting 指向同一 Site，D1／R2 均未配置。完整逐文件证据保存在 `v80-source-restore.json`。

此结果仅证明本地源码可恢复，不证明保存产物可以重新部署。未来真实演练须记录目标保存版本及产物摘要、新鲜权限、部署动作与终态、实际读取页面的版本、关键旅程、恢复耗时和回退失败处置。当前 V80 第 36 版的保存产物摘要为 `sha256:021661a97c556f6f032415a48a7422609c1a4e153daf7b321bed4df97ba324f0`；本轮仅查询该元数据，未下载重新验算保存包，也未将 V82 发布后再回滚。

## 继续验收的填写标准

预览恢复后按矩阵实际执行搜索人物、长小传、任官时间线、人物跳职官、年份切州镇、金石全文、切主题、返回保留定位。每项填写实际视口、主题、浏览器、日期、具体操作／预期／观察、截图或日志；失败保持失败，不由静态测试改为通过。真实 iPhone 另录型号、iOS／Safari、触控与软键盘、地址栏变化、横竖屏和缩放证据。

性能基线分别测人物检索至详情、长小传、金石全文，固定浏览器／设备、网络与 CPU 条件，区分冷／热状态并保留重复原始样本。未采集到的指标继续为空，不编造 LCP／INP／CLS 或上线性能结论。

内部项目、版本、部署及存储资源标识和服务地址不在本次证据中重复收录；目标关联通过既有 hosting 清单、此前 PR 回执、保存版本号和源码提交核验。

## 可追溯文件

- [外部回执与源码身份](release-metadata/v82-acceptance-20260908/external-evidence.json)
- [本次发布门禁终态](release-metadata/v82-acceptance-20260908/release-check.json)与[完整日志](release-metadata/v82-acceptance-20260908/release-check.log)
- [两次预览返回信息](release-metadata/v82-acceptance-20260908/preview-attempts.log)
- [72 项验收矩阵及真机／性能缺口](release-metadata/v82-acceptance-20260908/acceptance-matrix.json)
- [V80 本地源码恢复逐文件核验](release-metadata/v82-acceptance-20260908/v80-source-restore.json)
- [证据文件 SHA-256 清单](release-metadata/v82-acceptance-20260908/evidence-manifest.json)

现有 CI 运行 #5 的 artifact 查询返回空列表；它的成功终态已核实，但未取得该次运行的可下载产物包。本次复验日志与回执直接随仓库提交保存，和旧 CI 结果分别溯源。

日志中个别旧验证脚本使用“验收通过”或“release:check 全部通过”字样，须结合命令范围阅读：内部规范源检查不是完整外层发布命令，更不包含浏览器或真机操作。完整外层命令仅以 `release-check.json` 的最终退出码判断，视觉／真机状态仅由本记录和实际页面证据判断。

本记录所在提交的 CI 必须另查，不能沿用测试基线 c3ae8bb 的成功状态。所有新增证据位于仓库文档及 release-metadata，不进入读者数据。
